{
  "name": "stream-electron-ipc",
  "version": "1.0.9",
  "main": "lib/index",
  "types": "lib/index",
  "license": "MIT",
  "files": [
    "/lib"
  ],
  "scripts": {
    "build": "rimraf lib && tsc -p .",
    "prepublish": "yarn run build"
  },
  "devDependencies": {
    "@types/node": "^8.0.0",
    "electron": "^3.0.14",
    "rimraf": "^2.6.2",
    "tslint": "^5.11.0",
    "tslint-config-station": "^0.5.1",
    "typescript": "^3.2.2"
  }
}
